ant.importBuild('build.xml')

task gradleBase {
	doFirst {
		println "Hello from Gradle. Task ${name}"
		ant.properties['gradleExecution'] = true
	}
}

task gradleBaseDecision {
	doFirst {
		println "Hello from Gradle. Task ${name} "
	}
	onlyIf {
		!ant.properties.getOrDefault("skipGradle", "")
	}
}
